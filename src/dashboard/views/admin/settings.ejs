<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>筌ㅼ뮇?揶쎛 NO.1 獄쏆뮆以??볧??????곴틣 [24H]</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" type="image/png" href="/favicon.png">
</head>
<body>
  <div class="dashboard-container">
    <aside class="sidebar">
      <h1 class="sidebar-title">????癒곕솇疫???/h1>
      <nav class="sidebar-nav">
        <a href="/admin" class="nav-link"><span></span> ?遺쎌퍢 ??쇱읅</a>
        <a href="/admin/products" class="nav-link"><span></span> ??쀫?</a>
        <a href="/admin/logs" class="nav-link"><span></span> 嚥≪뮄??/a>
        <a href="/admin/settings" class="nav-link active"><span></span> ??쇱젟</a>
        <a href="/admin/embed" class="nav-link"><span></span> ?袁⑥퓢??/a>
        <a href="/admin/roles" class="nav-link"><span></span> ??釉?筌왖疫?/a>
      </nav>
      <div class="sidebar-footer">
        <div class="user-profile">
          <% if (user.avatar) { %>
            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png?size=128" class="user-avatar" alt="?袁⑥쨮??>
          <% } else { %>
            <img src="https://cdn.discordapp.com/embed/avatars/0.png" class="user-avatar" alt="?袁⑥쨮??>
          <% } %>
          <div class="user-name"><%= user.username || '?온?귐딆쁽' %></div>
        </div>
        <a href="/dashboard" class="nav-link"><span>?維</span> ??????뺣궖??/a>
        <a href="/auth/logout" class="logout-link-animated"><span>???/span> 嚥≪뮄??袁⑹뜍</a>
      </div>
    </aside>
    
    <main class="main-content">
      <div class="page-header">
        <h1 class="title-md">?癒?짗?겸뫗??嚥≪뮄????쇱젟</h1>
      </div>

      <div class="card mb-6">
        <div class="card-header">?逾???뺤쒔 筌띻낱寃?/div>
        <div class="flex gap-2">
          <input type="text" id="serverLink" placeholder="https://discord.gg/xxxxx" class="input" style="flex: 1;">
          <button onclick="saveServerLink()" class="btn btn-primary">????/button>
        </div>
      </div>

      <div class="settings-card-grid mb-6">
        <div class="card setting-equal-card">
          <div class="card-header">?裕??④쑴伊??類ｋ궖</div>
          
          <div class="card-inner-flow left-inputs-balance">
            <div class="input-group">
              <input type="text" id="bankInfo" placeholder="????곗구/?④쑴伊뽬린?딆깈 (?? ??? 123-456-789)" class="input">
            </div>
            <div class="input-group">
              <input type="number" id="minDeposit" placeholder="筌ㅼ뮇????껎닊 疫뀀뜆釉? class="input">
            </div>
          </div>
          
          <div class="card-action-row">
            <button onclick="saveBankSettings()" class="btn btn-primary" style="width: 100%;">????/button>
          </div>
        </div>
        
        <div class="card setting-equal-card">
          <div class="card-header">?諭?嚥≪뮄??筌?쑬瑗?ID</div>
          
          <div class="card-inner-flow right-inputs-balance">
            <div class="input-group">
              <input type="text" id="reviewLogChannel" placeholder="?닌됤꼻 ?袁㏓┛ 嚥≪뮄??筌?쑬瑗?ID" class="input">
            </div>
            <div class="input-group">
              <input type="text" id="purchaseLogChannel" placeholder="?닌됤꼻 嚥≪뮄??筌?쑬瑗?ID" class="input">
            </div>
            <div class="input-group">
                          <input type="text" id="chargeLogChannel" placeholder="?겸뫗??嚥≪뮄??筌?쑬瑗?ID (?癒?짗/??롫짗 ?⑤벊??" class="input">
                        </div>
                      </div>

                      <div class="card-action-row">
                        <button onclick="saveChannelSettings()" class="btn btn-primary" style="width: 100%;">????/button>
                      </div>
                    </div>
                  </div>

      <div class="card">
        <div class="card-header">?踰?SMS ?癒?짗?겸뫗??揶쎛??諭?/div>
        <div class="flex gap-4 mb-4">
          <button onclick="showGuide('android')" id="btnAndroid" class="btn btn-success">??덈굡嚥≪뮇???/button>
          <button onclick="showGuide('ios')" id="btnIos" class="btn btn-outline">?袁⑹뵠??/button>
        </div>
        <div id="guideContent" class="guide-box"></div>
      </div>
    </main>
  </div>

  <style>
    @media (min-width: 769px) {
      .settings-card-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 24px;
      }
    }

    .setting-equal-card {
      display: flex;
      flex-direction: column;
      height: 100%;
      min-height: 340px;
    }

    .setting-equal-card .card-header {
      margin-bottom: 12px;
    }

    .card-inner-flow {
      display: flex;
      flex-direction: column;
      flex: 1;
      gap: 12px; /* ??낆젾筌??????揶쏄쑨爰???類???띿쓺 ???뵬 */
    }

    .card-inner-flow.left-inputs-balance {
      justify-content: space-around;
      padding: 6px 0;
    }

    .card-inner-flow.right-inputs-balance {
      gap: 12px;
      justify-content: flex-start;
    }
    .setting-equal-card .input-group {
      margin-bottom: 0 !important;
    }

    .card-action-row {
      margin-top: auto;
      padding-top: 16px;
    }

    .guide-box {
      background: var(--bg-darker, #252830);
      border: 1px solid var(--border-color, #333);
      border-radius: 8px;
      padding: 1.25rem;
      color: #dcddde;
      font-family: monospace;
      white-space: pre-wrap;
      line-height: 1.6;
    }
  </style>

  <script>
    const keyToIdMap = {
      "BANK_INFO": "bankInfo",
      "MIN_DEPOSIT": "minDeposit",
      "REVIEW_CHANNEL_ID": "reviewLogChannel",
      "PURCHASE_LOG_CHANNEL": "purchaseLogChannel",
      "CHARGE_LOG_CHANNEL": "chargeLogChannel",
      "SERVER_LINK": "serverLink"
    };

    async function loadSettings() {
      try {
        const r = await fetch("/api/settings");
        const d = await r.json();
        
        if (d.settings && Array.isArray(d.settings)) {
          d.settings.forEach(s => {
            const targetId = keyToIdMap[s.key];
            if (targetId) {
              const el = document.getElementById(targetId);
              if (el) el.value = s.value;
            }
          });
        }
      } catch (e) {
        console.error("??쇱젟???븍뜄???삳뮉????쎈솭??됰뮸??덈뼄:", e);
      }
    }

    async function saveSetting(k, v) {
      return await fetch("/api/settings", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ key: k, value: v })
      });
    }

    async function saveServerLink() {
      const serverLink = document.getElementById("serverLink").value;
      await saveSetting("SERVER_LINK", serverLink);
      showToast("??뺤쒔 筌띻낱寃뺝첎? ???貫由??됰뮸??덈뼄.", "success");
    }

    async function saveBankSettings() {
      const bankInfo = document.getElementById("bankInfo").value;
      const minDeposit = document.getElementById("minDeposit").value;

      // ??API ?紐꾪뀱??癰귣쵎??筌ｌ꼶??
      await Promise.all([
        saveSetting("BANK_INFO", bankInfo),
        saveSetting("MIN_DEPOSIT", minDeposit)
      ]);
      
      showToast("?④쑴伊??類ｋ궖揶쎛 ???貫由??됰뮸??덈뼄.", "success");
    }

    async function saveChannelSettings() {
      const channelMappings = [
        { key: "REVIEW_CHANNEL_ID", value: document.getElementById("reviewLogChannel").value },
        { key: "PURCHASE_LOG_CHANNEL", value: document.getElementById("purchaseLogChannel").value },
        { key: "CHARGE_LOG_CHANNEL", value: document.getElementById("chargeLogChannel").value },
      ];

      // 癰귣쵎???遺욧퍕??곗쨮 ??얜즲 筌ㅼ뮇???
      const requests = channelMappings
        .filter(item => item.value !== undefined)
        .map(item => saveSetting(item.key, item.value));

      await Promise.all(requests);
      showToast("嚥≪뮄??筌?쑬瑗???쇱젟?????貫由??됰뮸??덈뼄.", "success");
    }

        const pushbulletGuide = `Pushbullet ?먮룞異⑹쟾 ?ㅼ젙

1. Pushbullet Android ?깆쓣 ?ㅼ튂?⑸땲??
2. Pushbullet ?깆뿉??Notification Mirroring??耳?땲??
3. .env??PUSHBULLET_TOKEN 媛믪쓣 ?낅젰?⑸땲??
4. ???臾몄옄/?뚮┝??Pushbullet ?ㅽ듃由쇱쑝濡??ㅼ뼱?ㅻ㈃ ?쒕쾭媛 ?먮룞?쇰줈 泥섎━?⑸땲??
5. SMS Forwarder, webhook/sms, WEBHOOK_SECRET? ???댁긽 ?ъ슜?섏? ?딆뒿?덈떎.`;

    function showGuide() {
      document.getElementById("guideContent").textContent = pushbulletGuide;
      document.getElementById("btnAndroid").className = "btn btn-success";
      document.getElementById("btnIos").className = "btn btn-outline";
    }

    loadSettings();
    showGuide();</script>
  <script src="/js/common.js"></script>
</body>
</html>